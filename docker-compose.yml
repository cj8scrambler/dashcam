# Vantrue N4S Dashcam Viewer
#
#   cp .env.example .env      # then edit DASHCAM_DATA_PATH and DASHCAM_CONFIG_PATH
#   docker compose build
#   docker compose run --rm dashcam-viewer python app.py adduser <name>   # create a login
#   docker compose up -d
#   open http://127.0.0.1:5000
#
#   docker compose logs -f
#   docker compose down            # stop (keeps the transcode cache and logins)
#   docker compose down -v         # also wipe the transcode cache (logins are a
#                                  # host bind mount, so they survive this)
#
# Two services: `dashcam-viewer` (the web app) and `sftp` (drop point for the
# remote offload daemon). Both share one .env in this directory.

# Pinned explicitly rather than left to default to the compose-file
# directory's basename. `docker compose down` is NOT profile-filtered the
# way `up` is - it matches and removes every container under the resolved
# PROJECT NAME regardless of which profile (if any) is active in that
# invocation (confirmed directly: `down` with no profile active still
# removed a container that was only ever started under one). Two checkouts
# of this repo whose directories happen to share a basename (or where a
# second instance's .env doesn't override this) would silently resolve to
# the SAME project name - so a `docker compose down` run against a "disabled"
# dev instance would tear down a live production instance's containers,
# `sftp` included, without any error or warning. A second instance's .env
# MUST set COMPOSE_PROJECT_NAME to something else, e.g. dashcam-dev - see
# .env.example. (For THIS repo's existing single-instance deployment the
# directory is already named `dashcam`, so this default changes nothing.)
name: "${COMPOSE_PROJECT_NAME:-dashcam}"

services:
  dashcam-viewer:
    build: .
    # Both default to the pre-existing literal names, so an existing single-
    # instance deployment's .env needs no changes. Set DASHCAM_IMAGE and
    # DASHCAM_CONTAINER_NAME in a second instance's .env (e.g. a dev/staging
    # copy running alongside production on the same host) so `docker compose
    # build` doesn't overwrite the other instance's image tag and `up` doesn't
    # collide on container name - both must be unique per Docker daemon
    # regardless of Compose project name.
    image: "${DASHCAM_IMAGE:-dashcam-viewer}"
    container_name: "${DASHCAM_CONTAINER_NAME:-dashcam-viewer}"
    restart: unless-stopped
    networks:
      - app-net

    # Left of the last colon is host bind (default: localhost only, so nginx on
    # the host is the only thing exposed). Set DASHCAM_BIND=0.0.0.0:5000 in .env
    # only if you're reaching it directly without a reverse proxy.
    ports:
      - "${DASHCAM_BIND:-127.0.0.1:5000}:5000"

    environment:
      DASHCAM_DATA_DIR: /data
      DASHCAM_RECORD_TZ: "${DASHCAM_RECORD_TZ:-America/Chicago}"
      DASHCAM_DISPLAY_TZ: "${DASHCAM_DISPLAY_TZ:-}"
      # Require a configured login before the app will start. Set to 0 only if
      # you deliberately want it open (e.g. it's already behind another gate).
      DASHCAM_REQUIRE_AUTH: "${DASHCAM_REQUIRE_AUTH:-1}"
      # Session cookies are Secure by default (assumes TLS at the proxy). Set to
      # 0 only if you're serving plain HTTP.
      DASHCAM_SECURE_COOKIE: "${DASHCAM_SECURE_COOKIE:-1}"
      # Transcoder: "none" (software libx264, default) or "nvenc" (NVIDIA GPU).
      # For "nvenc" also uncomment the `deploy:` block AND the capabilities line
      # just below.
      DASHCAM_HWACCEL: "${DASHCAM_HWACCEL:-none}"
      DASHCAM_HWACCEL_CONCURRENCY: "${DASHCAM_HWACCEL_CONCURRENCY:-3}"
      # NVIDIA_DRIVER_CAPABILITIES: "compute,video,utility"   # needed for nvenc
      # Pick up newly-synced footage without a restart:
      #  - RESCAN_INTERVAL: seconds between background re-scans (0 = off, 30s floor)
      #  - RELOAD_TRIGGER: a file whose mtime the app watches; the sftp uploader
      #    touches it after a batch, so the app re-scans within ~5s
      DASHCAM_RESCAN_INTERVAL: "${DASHCAM_RESCAN_INTERVAL:-300}"
      DASHCAM_RELOAD_TRIGGER: "${DASHCAM_RELOAD_TRIGGER:-/data/.reload}"
      # "Stops" (map marker + sidebar list for spans with no vehicle movement):
      # minimum duration under MAX_SPEED to count as a stop, not a red light.
      DASHCAM_STOP_MIN_MINUTES: "${DASHCAM_STOP_MIN_MINUTES:-5}"
      DASHCAM_STOP_MAX_SPEED_MPH: "${DASHCAM_STOP_MAX_SPEED_MPH:-2}"

    volumes:
      # The SD card / copy of it, containing Normal/ and GPS/. Read-only.
      - "${DASHCAM_DATA_PATH:?set DASHCAM_DATA_PATH in .env to your dashcam data directory}:/data:ro"
      # users.json + secret_key. A host bind mount (not a named volume) so the
      # logins are plain files you can back up and `docker compose down -v` can't
      # wipe. Must be writable by uid 1000 - see .env.example.
      - "${DASHCAM_CONFIG_PATH:?set DASHCAM_CONFIG_PATH in .env}:/config"
      # H.264 transcode cache - regenerable, fine to wipe.
      - dashcam-cache:/cache

    # The container runs as uid 1000. If it can't read /data (permission denied
    # on GPS load) or write /config, either fix ownership on the host
    # (chown 1000:1000 <config dir>; chmod -R a+rX <data dir>) or uncomment and
    # set this to a uid/gid that can - then also chown the dashcam-cache volume
    # or delete it once so it's recreated for that uid.
    # user: "1000:1000"

    # GPU transcoding: needs nvidia-container-toolkit on the host. Uncomment
    # this block, set DASHCAM_HWACCEL=nvenc, and uncomment
    # NVIDIA_DRIVER_CAPABILITIES above (the "video" cap is what exposes
    # NVENC/NVDEC - CUDA compute alone isn't enough).
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

  # SFTP drop point for the remote offload daemon (the machine near the car that
  # pulls footage off the camera). It writes into the SAME host directory the
  # viewer reads - the viewer keeps that mount read-only, this one has it rw.
  # Key-only auth, chrooted to /home/offload, no shell. See .env.example for the
  # one-time host-key + authorized-key setup.
  #
  # Only the real offload daemon should ever point at this - a second
  # (dev/staging) instance shouldn't run its own. Disabled by default via
  # Compose profiles: this service only starts when the "sftp" profile is
  # active (`COMPOSE_PROFILES=sftp` in .env, or `--profile sftp` on the CLI) -
  # a plain `docker compose up`/`up -d` with no profile set skips it entirely,
  # no need to name dashcam-viewer explicitly. Profiles only gate which
  # services *start*, though - Compose still interpolates every service's
  # variables up front regardless, so SFTP_BIND below defaults to a safe
  # loopback-only bind rather than being required, or a disabled instance's
  # `docker compose up` would still hard-fail on a missing var it'll never use.
  sftp:
    image: atmoz/sftp:alpine
    profiles: ["sftp"]
    # Parameterized for the same container-name-uniqueness reason as
    # dashcam-viewer, above - relevant if you ever DO want two sftp services.
    container_name: "${DASHCAM_SFTP_CONTAINER_NAME:-dashcam-sftp}"
    restart: unless-stopped
    networks:
      - app-net

    # Bind to the private VLAN interface only - never 0.0.0.0. The default
    # here is loopback-only specifically so an operator who sets
    # COMPOSE_PROFILES=sftp without also setting SFTP_BIND gets "unreachable
    # from the network" rather than "open to the network" - enabling it for
    # real still requires deliberately setting this to the VLAN address.
    ports:
      - "${SFTP_BIND:-127.0.0.1:2222}:22"

    # user:pass:uid:gid  -- empty pass = key-only. uid/gid must match the owner
    # of DASHCAM_DATA_PATH on the host (1000 here) so the viewer can read uploads.
    command: "offload::1000:1000"

    volumes:
      - "${DASHCAM_DATA_PATH:?}:/home/offload/dashcam"
      # the offload daemon's PUBLIC key(s) - atmoz builds authorized_keys from this dir
      - "${DASHCAM_CONFIG_PATH:?}/sftp/keys:/home/offload/.ssh/keys:ro"
      # persist SSH host keys so the daemon doesn't get host-key-changed warnings
      - "${DASHCAM_CONFIG_PATH:?}/sftp/ssh_host_ed25519_key:/etc/ssh/ssh_host_ed25519_key"
      - "${DASHCAM_CONFIG_PATH:?}/sftp/ssh_host_rsa_key:/etc/ssh/ssh_host_rsa_key"
networks:
  app-net:
    external: true

volumes:
  dashcam-cache:
